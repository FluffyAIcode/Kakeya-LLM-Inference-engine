# Agents

## Cursor Cloud specific instructions

This repository (`Kakeya-LLM-interfere-engine`) is currently empty — it contains only an Apache 2.0 `LICENSE` file. There are no source files, dependencies, build tools, tests, or services to run.

- **No update script dependencies**: The update script is a no-op (`echo "No dependencies to install"`). Update it once source code and a dependency manifest (e.g., `requirements.txt`, `package.json`) are added.
- **No lint/test/build/run commands** are available yet. Future agents should check for newly added configuration before attempting these steps.
